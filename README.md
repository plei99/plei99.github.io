# plei99.github.io

Personal website generated by a small OCaml/Dune static site generator.

The generator reads structured content from `data/`, renders Jingoo templates
from `templates/`, copies static assets from `static/`, and writes the complete
site to `public/` for GitHub Pages deployment.
