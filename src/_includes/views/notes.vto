<div class="wrap">
  <header class="page-head">
    <h1 class="page-title">
      <span class="marker marker--lilac" aria-hidden="true"></span>
      {{ ui[lang].notes }}
    </h1>
    <p class="page-lede">{{ ui[lang].notes_lede }}</p>
  </header>

  {{ include "views/bow.vto" }}

  <section class="section section--tight">
    <ul class="rows">
      {{ for note of notes }}
      {{ set n = lang === "zh" && note.zh ? { ...note, ...note.zh } : note }}
      <li class="row row--notes">
        <div class="row-date"><span>{{ n.date }}</span></div>
        <div>
          <h2 class="row-title"><a href="{{ note.url }}">{{ n.name }}</a></h2>
          <p class="row-body">{{ n.description.trim() }}</p>
          {{ if note.zh_translations }}
          <div class="row-links">
            {{ for t of note.zh_translations }}
            <a href="{{ t.url }}">{{ ui[lang].zh_translation }} · {{ t.translator }}</a>
            {{ /for }}
          </div>
          {{ /if }}
        </div>
        <div class="row-meta"><span>{{ n.place }}</span></div>
      </li>
      {{ /for }}
    </ul>
  </section>
</div>
