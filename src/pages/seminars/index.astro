---
import { getCollection } from "astro:content";
import Layout from "../../layouts/Layout.astro";
import PostRow from "../../components/Blog/PostRow.astro";

const posts = (await getCollection("blog"))?.sort(
  (blogEntryA, blogEntryB) =>
    (blogEntryB.data.pubDate || new Date()).getTime() -
    (blogEntryA.data.pubDate || new Date()).getTime()
);
---

<script>
  import { animate, type AnimationSequence } from "motion";
  import { loaderAnimation } from "../../lib/constants";

  const sequence = [loaderAnimation];

  animate(sequence as AnimationSequence);
</script>

<Layout
  description="Mathematician interested in higher-genus enumerative invariants"   title="Patrick Lei - Seminars"
>
  <main
    class="theme-text w-screen h-screen flex flex-col justify-start items-start max-w-3xl mx-auto p-8"
  >
    <a
      class="theme-button theme-border-strong border-1 border-solid px-4 py-2 mb-8 rounded-lg"
      href="/"
      >Back</a
    >
    <h1 class="text-4xl font-bold mb-4">Seminars I have organized</h1>
    <ul class="w-full">
      {
        posts?.map((post) => (
          <PostRow
            date={post.data.pubDate || new Date()}             id={post.id}             title={post.data.title || "No title"}
          />
        ))
      }
    </ul>
  </main>
</Layout>
